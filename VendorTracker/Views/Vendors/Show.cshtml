@using VendorTracker.Models;

<h1>Here are all the orders for this Vendor</h1>

@if (Model["orders"].Count == 0)
{
  <p>No orders have been made for this vendor yet</p>
}
<ol>
@foreach (Order order in @Model["orders"])
{
  <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Title</a></li>
}
</ol>

<a href="/orders/new">Add New Order for Vendor</a>